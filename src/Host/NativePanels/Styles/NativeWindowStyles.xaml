<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="QiTuToolWindowTitleBar" TargetType="Border">
    <Setter Property="Height" Value="32" />
    <Setter Property="Background" Value="{DynamicResource QiTuBgSurface}" />
    <Setter Property="BorderBrush" Value="{DynamicResource QiTuBorderDefault}" />
    <Setter Property="BorderThickness" Value="0,0,0,1" />
    <Setter Property="Padding" Value="12,0,0,0" />
  </Style>

  <Style x:Key="QiTuToolWindowTitle" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource QiTuTextPrimary}" />
    <Setter Property="FontSize" Value="{DynamicResource QiTuFontBase}" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="VerticalAlignment" Value="Center" />
  </Style>

  <Style x:Key="QiTuToolWindowStatus" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource QiTuTextSecondary}" />
    <Setter Property="FontSize" Value="{DynamicResource QiTuFontXs}" />
    <Setter Property="VerticalAlignment" Value="Center" />
  </Style>

  <Style x:Key="QiTuWindowVersionButton" TargetType="Button">
    <Setter Property="Height" Value="16" />
    <Setter Property="MinWidth" Value="34" />
    <Setter Property="Padding" Value="6,0" />
    <Setter Property="Background" Value="{DynamicResource QiTuBgElevated}" />
    <Setter Property="Foreground" Value="{DynamicResource QiTuTextTertiary}" />
    <Setter Property="BorderBrush" Value="{DynamicResource QiTuBorderDefault}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="FontSize" Value="{DynamicResource QiTuFontXs}" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Root"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{DynamicResource QiTuRadiusSm}">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource QiTuBorderFocus}" />
              <Setter Property="Foreground" Value="{DynamicResource QiTuAccentPrimary}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="QiTuToolWindowOuterBorder" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource QiTuBgBase}" />
    <Setter Property="BorderBrush" Value="{DynamicResource QiTuBorderDefault}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{DynamicResource QiTuRadiusMd}" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect BlurRadius="18"
                          ShadowDepth="2"
                          Opacity="0.16"
                          Color="#000000" />
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="QiTuOverlayPanel" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource QiTuBgSurface}" />
    <Setter Property="BorderBrush" Value="{DynamicResource QiTuBorderDefault}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{DynamicResource QiTuRadiusMd}" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect BlurRadius="18"
                          ShadowDepth="2"
                          Opacity="0.18"
                          Color="#000000" />
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="QiTuOverlayCloseButton" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{DynamicResource QiTuTextSecondary}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
          <ControlTemplate TargetType="Button">
          <Border x:Name="Root"
                  Background="{TemplateBinding Background}"
                  CornerRadius="0,6,0,0">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background" Value="{DynamicResource QiTuAccentDanger}" />
              <Setter Property="Foreground" Value="{DynamicResource QiTuTextOnAccent}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
